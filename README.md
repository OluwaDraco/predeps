# LB-SOCKET
This is a very crude implementation of the security tool SOCKET. It a small CLI that scans "node_modules" true and flags packages that execute code on the host machines (during npm install )

# Check list
# lifecycle scripts